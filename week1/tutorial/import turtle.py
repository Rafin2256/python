import turtle

window = turtle.Screen()
window.setup(0.5, 0.75) #set window size 
window.bgcolor(0.5, 0.2, 0.7) #set window color
window.title ("First python program")  # First python program

turtle.done()